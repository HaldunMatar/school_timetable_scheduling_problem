# توليد ملف تنفيذي (exe لويندوز + app للماك) — بدون أي جهاز ويندوز، وبدون طرفية

هذا الدليل يشرح كيف تحصل على ملفين تنفيذيين حقيقيين — **exe يعمل على أي جهاز
ويندوز**، و**app يعمل على أي جهاز ماك** — بدون تثبيت بايثون على جهاز
المستخدم النهائي، وبدون فتح الطرفية (Terminal) في أي خطوة، لا عندك ولا عند
من تُرسل له البرنامج.

الفكرة: GitHub (منصة مجانية لاستضافة الكود) توفّر خدمة اسمها **GitHub
Actions**، وهي أجهزة ويندوز وماك حقيقية تعمل في السحابة مجاناً للحسابات
الشخصية. نرفع كود البرنامج إليها مرة واحدة، فتقوم تلقائياً ببناء الملفين
باستخدام سكربت البناء الجاهز في هذا المجلد (`app.spec`)، وتترك لك الملفين
جاهزين للتنزيل. هذا يحصل من خلال متصفح الإنترنت فقط، دون أي شيء آخر.

---

## الخطوة 1: إنشاء حساب GitHub (إذا لم يكن لديك واحد)

1. افتح [github.com](https://github.com) وأنشئ حساباً مجانياً (بريد إلكتروني
   + كلمة مرور، مثل أي حساب آخر).

## الخطوة 2: إنشاء "مستودع" (Repository) جديد

1. بعد تسجيل الدخول، اضغط زر **New** الأخضر (أو أيقونة + أعلى الصفحة ثم
   **New repository**).
2. اختر اسماً (مثلاً `teacher-scheduler`)، واتركه **Public** أو اجعله
   **Private** إن رغبت (كلاهما يعمل مع GitHub Actions مجاناً).
3. **لا تفعّل** أي خيار من خيارات "Add a README" أو ".gitignore" في هذه
   الشاشة — اترك كل شيء فارغاً واضغط **Create repository**.

## الخطوة 3: رفع ملفات البرنامج (بدون طرفية — سحب وإفلات فقط)

1. في صفحة المستودع الجديد الفارغة، ستجد رابط **uploading an existing
   file** — اضغط عليه (أو من تبويب "Add file" اختر "Upload files").
2. **افتح مجلد `app` الذي بين يديك الآن، وحدد كل محتوياته** (main.py،
   gui.py، scheduler.py، pdf_gen.py، requirements.txt، app.spec، README.md،
   BUILD.md، ومجلدي `assets` و`data` كاملين، ومجلد `.github` كاملاً — **هذا
   المجلد قد يكون مخفياً في بعض متصفحات الملفات لأن اسمه يبدأ بنقطة، تأكد
   من إظهار الملفات المخفية قبل السحب**) ثم اسحبها كلها إلى صفحة الرفع.
   **مهم:** ارفع *محتويات* مجلد `app`، وليس مجلد `app` نفسه كمجلد واحد —
   أي يجب أن يظهر `main.py` مباشرة في جذر المستودع، لا داخل مجلد فرعي اسمه
   `app`.
3. انزل للأسفل واضغط **Commit changes** (اترك الإعدادات الافتراضية كما
   هي).

## الخطوة 4: انتظار البناء التلقائي

1. بمجرد رفع الملفات، تبدأ عملية البناء تلقائياً. اذهب إلى تبويب
   **Actions** أعلى صفحة المستودع.
2. ستجد عملية باسم "Build Desktop App" قيد التشغيل (دائرة صفراء تدور).
   يستغرق البناء عادة **5 إلى 10 دقائق** لكلا النظامين معاً.
3. عند اكتمالها بنجاح تتحول العلامة إلى ✅ أخضر.

## الخطوة 5: تنزيل الملفين الجاهزين

1. اضغط على عملية البناء المكتملة (✅) لفتحها.
2. في أسفل صفحتها ستجد قسم **Artifacts** فيه ملفان:
   - **TeacherScheduler-Windows** → بداخله `TeacherScheduler.exe` — انسخه
     لأي جهاز ويندوز وشغّله بالضغط المزدوج مباشرة، بلا تثبيت بايثون.
   - **TeacherScheduler-macOS** → بداخله `TeacherScheduler.app` — انسخه
     لأي جهاز ماك وشغّله بالضغط المزدوج.
3. اضغط على كل اسم لتنزيله كملف مضغوط (zip)، ثم فك الضغط.

---

## ملاحظة مهمة لمستخدمي الماك (أول تشغيل فقط)

بما أن التطبيق غير موقّع بشهادة مطوّر رسمية من Apple (وهي خدمة مدفوعة)،
سيظهر عند أول تشغيل على أي جهاز ماك تحذير أن "المطوّر غير موثّق" ويرفض
النظام فتحه بالضغط المزدوج العادي. الحل (يُفعل **مرة واحدة فقط** لكل جهاز):

1. بدل الضغط المزدوج، اضغط بزر الفأرة الأيمن (أو Control + ضغط) على أيقونة
   `TeacherScheduler.app` واختر **Open** من القائمة.
2. سيظهر نفس التحذير لكن هذه المرة بخيار **Open** إضافي — اضغطه.
3. من هذه المرة فصاعداً يعمل التطبيق بالضغط المزدوج العادي دون أي تحذير
   على هذا الجهاز.

(على ويندوز قد يظهر تحذير مشابه من "Windows Defender SmartScreen" لنفس
السبب — الحل هناك: اضغط "More info" ثم "Run anyway".)

## إعادة البناء لاحقاً

في أي وقت تُحدّث فيه ملفات البرنامج، ارفع الملفات المعدَّلة فقط إلى نفس
المستودع (من "Add file > Upload files" مجدداً) — يعيد GitHub البناء
تلقائياً بعد كل رفع. يمكنك أيضاً تشغيل بناء جديد يدوياً دون أي تعديل من
تبويب Actions > Build Desktop App > زر **Run workflow**.
